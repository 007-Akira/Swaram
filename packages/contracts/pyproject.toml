[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[project]
name = "swaram-contracts"
version = "0.0.0"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["python/swaram_contracts"]
